apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: subscriber-data-pvc
  namespace: pubsub-demo
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
